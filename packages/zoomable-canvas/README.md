# @sanjo/zoomable-canvas

This work is devoted to God.

## How to run

From the repository root directory:

```sh
yarn workspace @sanjo/zoomable-canvas development
```

## How to build

From the repository root directory:

```sh
yarn workspace @sanjo/zoomable-canvas build
```
