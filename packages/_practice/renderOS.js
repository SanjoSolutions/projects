import React from 'react';
import { render } from 'react-dom';
import { OS } from './OS.js';
export function renderOS(container) {
    render(React.createElement(OS, null), container);
}
//# sourceMappingURL=renderOS.js.map