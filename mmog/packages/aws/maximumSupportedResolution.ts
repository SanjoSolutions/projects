const MAXIMUM_SUPPORTED_RESOLUTION = {
  width: 2560,
  height: 1440,
}
export const HALF_WIDTH = Math.ceil(MAXIMUM_SUPPORTED_RESOLUTION.width / 2)
export const HALF_HEIGHT = Math.ceil(MAXIMUM_SUPPORTED_RESOLUTION.height / 2)
