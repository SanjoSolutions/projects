describe('splitFunctionsIntoSeparateFiles', () => {
  it('splits the functions of a given file into separate files. Each file with the same name as the function.', () => {
    // To consider:
    //   imports
    //   exports
  })
})
